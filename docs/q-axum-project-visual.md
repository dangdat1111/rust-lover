# Axum Project — Minh Hoạ Trực Quan

> Companion visual cho [axum-project.md](./axum-project.md). Đọc song song.

---

## 1. Bức tranh lớn — axum Project Architecture

```
                  PRODUCTION-READY AXUM PROJECT
       ┌────────────────────────────────────────────────────────┐
       │                                                        │
       │   ┌──────────────────────────────────────────────┐     │
       │   │  axum Router + Tower Layers                  │     │
       │   │                                              │     │
       │   │  Routes → Middleware stack → Handlers        │     │
       │   └──────────────────────────────────────────────┘     │
       │                       │                                │
       │                       ▼                                │
       │   ┌──────────────────────────────────────────────┐     │
       │   │  AppState (Arc<Inner>)                       │     │
       │   │   ├─ PgPool (DB)                             │     │
       │   │   ├─ Redis client                            │     │
       │   │   ├─ Config (Arc)                            │     │
       │   │   ├─ Cache (moka)                            │     │
       │   │   └─ Metrics registry                        │     │
       │   └──────────────────────────────────────────────┘     │
       │                                                        │
       │   APPLIES 15 chapters:                                 │
       │   ┌─────────────────────────────────────────────────┐  │
       │   │ trait → IntoResponse, Service                   │  │
       │   │ generic → State<T>, Extractor<T>                │  │
       │   │ async → tokio, sqlx                             │  │
       │   │ error-handling → AppError + IntoResponse        │  │
       │   │ smart-pointers → Arc, PgPool                    │  │
       │   │ observability → tracing + prometheus            │  │
       │   │ testing → unit + integration + e2e              │  │
       │   │ performance → criterion, profile, optimize      │  │
       │   └─────────────────────────────────────────────────┘  │
       │                                                        │
       └────────────────────────────────────────────────────────┘
```

---

## 2. Tower Service abstraction

```
   ┌────────────────────────────────────────────────────────┐
   │  pub trait Service<Request> {                          │
   │      type Response;                                    │
   │      type Error;                                       │
   │      type Future: Future<Output = Result<...>>;        │
   │                                                        │
   │      fn poll_ready(...) -> Poll<...>;                  │
   │      fn call(&mut self, req: Request) -> Self::Future; │
   │  }                                                     │
   └────────────────────────────────────────────────────────┘
   
   
   Onion-style layering:
   ─────────────────────
   
   ┌─────────────────────────────────────────────────┐
   │  Outermost layer (e.g., TraceLayer)             │
   │  ┌──────────────────────────────────────────┐   │
   │  │  Layer 2 (e.g., CorsLayer)               │   │
   │  │  ┌────────────────────────────────────┐  │   │
   │  │  │  Layer 3 (e.g., TimeoutLayer)      │  │   │
   │  │  │  ┌──────────────────────────────┐  │  │   │
   │  │  │  │  Router → Handler             │  │  │   │
   │  │  │  └──────────────────────────────┘  │  │   │
   │  │  └────────────────────────────────────┘  │   │
   │  └──────────────────────────────────────────┘   │
   └─────────────────────────────────────────────────┘
   
   Request flow:  TraceLayer → CorsLayer → TimeoutLayer → Handler
   Response flow: TraceLayer ← CorsLayer ← TimeoutLayer ← Handler
```

---

## 3. Project structure layout

```
   my-api/
   ├── Cargo.toml                # workspace
   ├── README.md
   ├── .env.example
   ├── Dockerfile
   ├── compose.yaml              # local dev
   │
   ├── crates/
   │   │
   │   ├── api/                  ← THIN: web layer
   │   │   ├── Cargo.toml
   │   │   └── src/
   │   │       ├── main.rs       ← entry, init telemetry
   │   │       ├── lib.rs        ← for integration tests
   │   │       ├── routes/
   │   │       │   ├── mod.rs
   │   │       │   ├── users.rs
   │   │       │   ├── auth.rs
   │   │       │   └── health.rs
   │   │       ├── middleware/
   │   │       │   └── auth.rs
   │   │       ├── error.rs      ← AppError + IntoResponse
   │   │       ├── state.rs      ← AppState
   │   │       └── config.rs
   │   │
   │   ├── domain/               ← BUSINESS: pure logic
   │   │   ├── Cargo.toml
   │   │   └── src/
   │   │       ├── lib.rs
   │   │       ├── user.rs       ← User entity, rules
   │   │       └── auth.rs       ← password hash, JWT
   │   │
   │   └── db/                   ← INFRA: data access
   │       ├── Cargo.toml
   │       ├── migrations/       ← sqlx migrations
   │       │   └── *.sql
   │       └── src/
   │           ├── lib.rs
   │           ├── user.rs       ← UserRepo
   │           └── connection.rs
   │
   └── tests/                    ← E2E integration tests
       └── integration.rs
   
   
   Why workspace:
   ──────────────
   ✅ Separate concerns
   ✅ Faster compile (change api ≠ rebuild db)
   ✅ Reusable (extract crate for CLI/worker)
   ✅ Independent testing per layer
```

---

## 4. Layered architecture

```
   ┌──────────────────────────────────────────────┐
   │                                              │
   │  HTTP Routes (api crate)                     │  ← THIN
   │  Parse request, extractors, format response  │
   │                                              │
   ├──────────────────────────────────────────────┤
   │                                              │
   │  Application Service                         │
   │  Orchestrate business logic + repositories   │
   │                                              │
   ├──────────────────────────────────────────────┤
   │                                              │
   │  Domain Logic (domain crate)                 │  ← PURE
   │  Business rules, validation, NO web/DB knows │
   │                                              │
   ├──────────────────────────────────────────────┤
   │                                              │
   │  Repository (db crate)                       │  ← I/O
   │  sqlx queries, DB transactions               │
   │                                              │
   └──────────────────────────────────────────────┘
   
   
   Dependency rule: each layer only depends on layers BELOW.
   
   ┌────────────────────────────────────┐
   │  api depends on domain + db        │
   │  domain depends on nothing         │
   │  db depends on nothing             │
   │                                    │
   │  → domain pure & easy to unit test │
   │  → db can mock if needed           │
   │  → api orchestrates                │
   └────────────────────────────────────┘
```

---

## 5. Request lifecycle

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   HTTP REQUEST                                           │
   │      │                                                   │
   │      ▼                                                   │
   │   ┌──────────────────┐                                   │
   │   │ TraceLayer        │ ← create span, set request_id   │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ CorsLayer         │ ← check origin                   │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Compression       │ ← decompress request body        │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Timeout           │ ← start countdown                │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Router            │ ← match URL pattern              │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Custom middleware │ ← e.g., auth                     │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Extractors run    │ ← Path, Query, Json, State       │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Handler           │ ← business logic                 │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ Result → IntoResp │ ← AppError → JSON error          │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼  (response flows back through layers)       │
   │   ┌──────────────────┐                                   │
   │   │ Compression       │ ← gzip response body             │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   ┌──────────────────┐                                   │
   │   │ TraceLayer        │ ← log status + latency           │
   │   └────────┬─────────┘                                   │
   │            │                                             │
   │            ▼                                             │
   │   HTTP RESPONSE                                          │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
```

---

## 6. Extractors visualization

```
   ┌─────────────────────────────────────────────────────────┐
   │                                                         │
   │  HTTP REQUEST: POST /users/42/orders                    │
   │  Headers: Authorization: Bearer ...                     │
   │  Query: ?status=active                                  │
   │  Body: {"item": "book", "qty": 2}                       │
   │                                                         │
   └──────────────┬──────────────────────────────────────────┘
                  │
                  ▼
   ┌─────────────────────────────────────────────────────────┐
   │                                                         │
   │  async fn create_order(                                 │
   │      State(state): State<AppState>,    ← AppState clone │
   │      auth: AuthUser,                    ← parse JWT     │
   │      Path(user_id): Path<u64>,          ← from URL "42" │
   │      Query(q): Query<Filter>,           ← from ?status= │
   │      Json(body): Json<CreateOrder>,     ← from body     │
   │  ) -> Result<Json<Order>, AppError> {                   │
   │      // all extracted before handler runs               │
   │      // type-safe, validated                            │
   │  }                                                      │
   │                                                         │
   └─────────────────────────────────────────────────────────┘
   
   
   Extractor types:
   ────────────────
   
   ┌──────────────────┬─────────────────────────────────────┐
   │ Extractor        │ What                                │
   ├──────────────────┼─────────────────────────────────────┤
   │ Path<T>          │ URL path params                     │
   │ Query<T>         │ ?key=value query string             │
   │ Json<T>          │ application/json body               │
   │ Form<T>          │ application/x-www-form-urlencoded   │
   │ Multipart        │ multipart/form-data (file upload)   │
   │ State<T>         │ app state (Arc-shared)              │
   │ Extension<T>     │ request-scoped middleware-injected  │
   │ HeaderMap        │ all headers                         │
   │ TypedHeader<T>   │ specific typed header               │
   │ ConnectInfo<T>   │ client info (IP, etc.)              │
   │ Bytes / String   │ raw body                            │
   │ AuthUser (custom)│ your own extractor                  │
   └──────────────────┴─────────────────────────────────────┘
   
   
   ⚠️ Body extractors (Json, Form, Bytes) MUST be LAST!
      Only ONE body extractor per handler.
```

---

## 7. AppState & smart pointers

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   #[derive(Clone)]                                       │
   │   pub struct AppState {                                  │
   │       pub db: PgPool,            ← Arc internally        │
   │       pub redis: redis::Client,  ← Arc internally        │
   │       pub config: Arc<Config>,   ← explicit Arc          │
   │       pub cache: Arc<Cache<...>>,                        │
   │   }                                                      │
   │                                                          │
   │   // Clone = bumping Arc refcount only — CHEAP           │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Memory layout:
   ──────────────
   
   Each request handler:
   ┌────────────────────┐
   │ Handler frame      │
   │   state: AppState  │ ← bumped Arc refcounts
   │     db: PgPool ────┼──────┐
   │     config:Arc<>─┐ │      │
   └──────────────────┼─┘      │
                      │        │
                      ▼        ▼
                   Heap: shared instances
                   ┌────────────┐
                   │ Config     │
                   │  (Arc)     │
                   └────────────┘
                   ┌────────────┐
                   │ PgPool     │
                   │  (already  │
                   │   Arc-like)│
                   └────────────┘
   
   ⟹ Multiple requests share underlying state via Arc
   ⟹ Clone AppState = bump refcount (~ns)
   
   
   State<T> extractor:
   ───────────────────
   
   axum injects state via .with_state(state):
   
   let app = Router::new()
       .route("/users", get(handler))
       .with_state(state);          ← Arc bumped per request
```

---

## 8. Middleware onion model

```
   ┌────────────────────────────────────────────────────────────┐
   │                                                            │
   │   let app = Router::new()                                  │
   │       .route("/", get(handler))                            │
   │       .layer(layer_a)     ← LAST applied = OUTERMOST       │
   │       .layer(layer_b)                                      │
   │       .layer(layer_c);    ← FIRST applied = INNERMOST      │
   │                                                            │
   └────────────────────────────────────────────────────────────┘
   
   
   Execution order:
   ────────────────
   
                Request flow ──►
   ┌──────────┐       ┌──────────┐       ┌──────────┐       ┌─────────┐
   │ layer_a   │ ──►   │ layer_b   │ ──►   │ layer_c   │ ──►   │ handler │
   │ (outer)   │       │ (middle)  │       │ (inner)   │       │         │
   └──────────┘       └──────────┘       └──────────┘       └─────────┘
        ▲                  ▲                  ▲                  │
        │                  │                  │                  │
   ┌────┴─────┐       ┌────┴─────┐       ┌────┴─────┐       Response
   │ layer_a   │ ◄──── │ layer_b   │ ◄──── │ layer_c   │ ◄────  back
   │           │       │           │       │           │
   └──────────┘       └──────────┘       └──────────┘
                ◄── Response flow
   
   
   Typical order:
   ──────────────
   
   .layer(TraceLayer)              ← outermost (logs everything)
   .layer(CorsLayer)               ← check origin
   .layer(CompressionLayer)        ← compress response
   .layer(TimeoutLayer)            ← request timeout
   .layer(custom_middleware::auth) ← auth check
                                   ← handler runs (innermost)
   
   📌 Outer = first to see request, last to see response.
```

---

## 9. Error handling flow

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   #[derive(Error, Debug)]                                │
   │   pub enum AppError {                                    │
   │       #[error("user not found")]                         │
   │       UserNotFound,                                      │
   │                                                          │
   │       #[error("unauthorized")]                           │
   │       Unauthorized,                                      │
   │                                                          │
   │       #[error("internal")]                               │
   │       Internal(#[from] anyhow::Error),                   │
   │                                                          │
   │       #[error("database")]                               │
   │       Db(#[from] sqlx::Error),                           │
   │   }                                                      │
   │                                                          │
   │   impl IntoResponse for AppError {                       │
   │       fn into_response(self) -> Response {               │
   │           let (status, code, msg) = match &self {        │
   │               AppError::UserNotFound => (404, "...", ..),│
   │               AppError::Unauthorized => (401, "...", ..),│
   │               AppError::Internal(e) => {                 │
   │                   tracing::error!(?e);  ← LOG full!     │
   │                   (500, "INTERNAL", "...")               │
   │               }                                          │
   │               ...                                        │
   │           };                                             │
   │           (status, Json(json!({"error": {                │
   │               "code": code, "message": msg               │
   │           }}))).into_response()                          │
   │       }                                                  │
   │   }                                                      │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Handler propagation with `?`:
   ──────────────────────────────
   
   async fn handler() -> Result<Json<User>, AppError> {
       let user = db.fetch().await?;             // sqlx::Error → Db
       let parsed = parse_data(&user)?;          // anyhow::Error → Internal
       
       parsed.ok_or(AppError::UserNotFound)?     // None → NotFound
   }
   
   
   Client response (consistent):
   ─────────────────────────────
   
   {
     "error": {
       "code": "USER_NOT_FOUND",     ← stable for client to handle
       "message": "user not found"    ← human-readable
     }
   }
   
   Status 404
   
   
   ⚠️ Internal errors:
   ────────────────────
   
   Server log:    full stack trace, SQL query, etc.
   Client sees:   generic "Internal server error" + code "INTERNAL"
                  → DON'T leak internals (security)
```

---

## 10. Authentication flow (JWT)

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   LOGIN flow:                                            │
   │                                                          │
   │   Client                          Server                 │
   │     │                                │                   │
   │     │ POST /login                    │                   │
   │     │ {email, password}              │                   │
   │     │───────────────────────────────►│                   │
   │     │                                │                   │
   │     │                                │ 1. lookup user    │
   │     │                                │ 2. verify Argon2  │
   │     │                                │ 3. create JWT     │
   │     │                                │                   │
   │     │ {token, user}                  │                   │
   │     │◄───────────────────────────────│                   │
   │     │                                │                   │
   │     │                                │                   │
   │   PROTECTED REQUEST:                 │                   │
   │     │                                │                   │
   │     │ GET /profile                   │                   │
   │     │ Authorization: Bearer eyJh...  │                   │
   │     │───────────────────────────────►│                   │
   │     │                                │                   │
   │     │              AuthUser extractor:                   │
   │     │                                │                   │
   │     │  1. Get "Authorization" header                     │
   │     │  2. Strip "Bearer "                                │
   │     │  3. verify_token(token, secret)                    │
   │     │  4. Parse claims → AuthUser                        │
   │     │                                │                   │
   │     │  5. handler(AuthUser { user_id, role })            │
   │     │                                │                   │
   │     │ {profile data}                 │                   │
   │     │◄───────────────────────────────│                   │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   JWT structure:
   ──────────────
   
   eyJhbGciOiJIUzI1NiJ9. eyJzdWIiOiI0Mi.....   .signature
   ─────────────────────  ──────────────────────  ─────────
        Header              Payload (claims)       HMAC signature
                            {sub, exp, iat, role}
   
   Verify on server:
   - Decode + check signature (HMAC with secret)
   - Check exp (not expired)
   - Trust claims → user_id, role
   
   
   Role-based authorization:
   ─────────────────────────
   
   pub struct AdminUser(pub AuthUser);   ← wrap, restrict
   
   impl FromRequestParts for AdminUser {
       async fn from_request_parts(...) {
           let user = AuthUser::from_request_parts(...).await?;
           match user.role {
               Role::Admin => Ok(AdminUser(user)),
               _ => Err(AppError::Forbidden)
           }
       }
   }
   
   async fn admin_only(_: AdminUser) { ... }   ← only admin reaches here
```

---

## 11. Database integration với sqlx

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   query_as!(User, "SELECT * FROM users WHERE id = $1", id)│
   │                                                          │
   │   COMPILE TIME:                                          │
   │   ┌───────────────────────────────────────────────────┐  │
   │   │ sqlx macro:                                       │  │
   │   │  1. Connect to DATABASE_URL                       │  │
   │   │  2. Run EXPLAIN on query                          │  │
   │   │  3. Verify columns match User struct              │  │
   │   │  4. Verify $1 type matches `id`                   │  │
   │   │  5. Generate typed code                           │  │
   │   │                                                   │  │
   │   │  Result: compile error if SQL wrong               │  │
   │   └───────────────────────────────────────────────────┘  │
   │                                                          │
   │   RUNTIME:                                               │
   │   ┌───────────────────────────────────────────────────┐  │
   │   │ Send prepared statement to DB                     │  │
   │   │ Bind parameters                                   │  │
   │   │ Parse rows into User struct                       │  │
   │   │ Return Vec<User> or fetch_one() etc.              │  │
   │   └───────────────────────────────────────────────────┘  │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Offline mode (for CI without DB):
   ─────────────────────────────────
   
   $ cargo install sqlx-cli
   $ cargo sqlx prepare      ← generates .sqlx/*.json
   $ git add .sqlx/
   
   CI build: uses cached .sqlx/, no DB needed.
   
   
   Repository pattern:
   ───────────────────
   
   ┌──────────────────────────────────────────────────┐
   │ UserRepo {                                       │
   │   pool: PgPool,                                  │
   │ }                                                │
   │                                                  │
   │ impl UserRepo {                                  │
   │   async fn find_by_id(...) -> Result<...> {...}  │
   │   async fn find_by_email(...) -> Result<...> {...│
   │   async fn create(...) -> Result<...> {...}      │
   │   async fn update(...) -> Result<...> {...}      │
   │ }                                                │
   └──────────────────────────────────────────────────┘
   
   ⟹ Encapsulate SQL. Handlers call .find_by_id(), not raw SQL.
   ⟹ Easier to mock for unit tests.
   
   
   Transactions:
   ─────────────
   
   let mut tx = pool.begin().await?;
   user_create(&mut tx, ...).await?;
   profile_create(&mut tx, ...).await?;
   tx.commit().await?;
   
   // If commit not called, ALL operations rolled back
   // (drop tx without commit = rollback)
```

---

## 12. Observability stack visualization

```
   ┌──────────────────────────────────────────────────────────┐
   │                  YOUR AXUM APP                           │
   │                                                          │
   │  TraceLayer  → creates span per request                  │
   │     ↓                                                    │
   │  #[instrument] on handlers                               │
   │     ↓                                                    │
   │  tracing::info!() inside                                 │
   │     ↓                                                    │
   │  metrics::counter!()                                     │
   │     ↓                                                    │
   │  tracing-subscriber:                                     │
   │   ├─ EnvFilter (info, sqlx=warn, ...)                    │
   │   ├─ fmt::json (stdout)                                  │
   │   └─ opentelemetry layer (OTLP)                          │
   │     ↓                                                    │
   │  metrics-exporter-prometheus → :9000/metrics              │
   │                                                          │
   └─┬──────────────────────────┬───────────────────┬─────────┘
     │ stdout                    │ OTLP gRPC         │ /metrics
     ▼                           ▼                   ▼
   ┌──────────┐         ┌────────────────┐    ┌─────────────┐
   │ Promtail │         │ OTel Collector │    │ Prometheus  │
   │ Vector   │         │                │    │ scraper     │
   │ Fluentd  │         └────┬───────────┘    └─────┬───────┘
   └────┬─────┘              │                      │
        ▼                    ▼                      ▼
   ┌──────────┐         ┌──────────────┐    ┌──────────────┐
   │  Loki    │         │  Tempo /     │    │  Prometheus  │
   │  (logs)  │         │  Jaeger      │    │  /Mimir      │
   └────┬─────┘         └──────┬───────┘    └──────┬───────┘
        │                      │                   │
        └──────────────────────┼───────────────────┘
                               │
                               ▼
                      ┌────────────────┐
                      │    GRAFANA     │
                      │  Dashboards    │
                      │  Alerts        │
                      │  Correlation   │
                      └────────────────┘
   
   
   Request flow with correlation:
   ──────────────────────────────
   
   1. Request arrives with X-Request-ID (or generate UUID)
   2. TraceLayer creates span with request_id
   3. All logs inside that span include request_id
   4. trace_id from OpenTelemetry also linked
   5. Metrics tagged with route, method, status
   
   Debug: search by request_id → see EVERYTHING that happened
```

---

## 13. Testing strategy

```
   ┌──────────────────────────────────────────────────────────┐
   │  Test Pyramid (applied to axum):                         │
   │                                                          │
   │              /\                                          │
   │             /  \                                         │
   │            /E2E \    ← reqwest → real HTTP → real DB    │
   │           /──────\     Slow, fragile, FEW                │
   │          /        \                                      │
   │         /Integration\   ← oneshot Router, test DB       │
   │        /────────────\     Moderate speed, MORE           │
   │       /              \                                   │
   │      /  Unit (domain) \   ← pure functions, no I/O      │
   │     /──────────────────\    FAST, MANY                   │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Unit test (domain crate):
   ─────────────────────────
   
   #[test]
   fn validate_password() {
       assert!(validate("password1").is_ok());
       assert_eq!(validate("abc"), Err("too short"));
   }
   
   ⟹ Pure function, no DB, no HTTP, ms each.
   
   
   Integration test (oneshot Router):
   ──────────────────────────────────
   
   #[tokio::test]
   async fn create_user_endpoint() {
       let state = test_state().await;   ← test DB
       let app = build_app(state).await;
       
       let resp = app.oneshot(           ← Tower service oneshot
           Request::builder()
               .method("POST")
               .uri("/users")
               .header("content-type", "application/json")
               .body(Body::from(r#"{"email":"...","password":"..."}"#))
               .unwrap()
       ).await.unwrap();
       
       assert_eq!(resp.status(), 201);
   }
   
   ⟹ Tests full handler stack, no port binding.
   
   
   E2E test (real HTTP):
   ─────────────────────
   
   #[tokio::test]
   async fn e2e_full_flow() {
       let listener = TcpListener::bind("127.0.0.1:0").await.unwrap();
       let port = listener.local_addr().unwrap().port();
       tokio::spawn(async move { axum::serve(listener, app).await });
       
       let client = reqwest::Client::new();
       // ... make HTTP calls with reqwest ...
   }
   
   ⟹ Tests serialization, auth, TLS, etc. Real HTTP cycle.
   
   
   sqlx::test for DB tests:
   ────────────────────────
   
   #[sqlx::test]
   async fn test_repo(pool: PgPool) {
       // pool = fresh test DB
       // auto-rollback after test
       let repo = UserRepo::new(pool);
       let user = repo.create("alice@test.com", "hash").await.unwrap();
       assert_eq!(user.email, "alice@test.com");
   }
```

---

## 14. Production deployment

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │  BUILD                       DEPLOY                      │
   │  ─────                       ──────                      │
   │  ┌────────────────┐         ┌───────────────────┐        │
   │  │ Multi-stage    │         │ Docker / OCI      │        │
   │  │ Dockerfile     │ ──build►│ image (50-100MB)  │        │
   │  │ • rust:slim    │         └─────────┬─────────┘        │
   │  │ • cargo build  │                   │                  │
   │  │ • debian:slim  │                   ▼                  │
   │  │ • copy binary  │         ┌───────────────────┐        │
   │  └────────────────┘         │ Kubernetes /      │        │
   │                             │ ECS / Cloud Run   │        │
   │                             │                   │        │
   │                             │ • replicas: 3     │        │
   │                             │ • health probes   │        │
   │                             │ • resource limits │        │
   │                             │ • secrets injected│        │
   │                             └─────────┬─────────┘        │
   │                                       │                  │
   │                                       ▼                  │
   │                             ┌───────────────────┐        │
   │                             │ Load Balancer     │        │
   │                             │ (Ingress/ALB)     │        │
   │                             │ + TLS termination │        │
   │                             └─────────┬─────────┘        │
   │                                       │                  │
   │                                       ▼                  │
   │                              public internet             │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Health probes:
   ──────────────
   
   /health      — always returns 200 (liveness check)
                  K8s restarts pod if fails
   
   /ready       — checks DB + dependencies (readiness check)
                  Pod removed from LB if fails
   
   /metrics     — Prometheus endpoint
   
   
   Graceful shutdown flow:
   ───────────────────────
   
   Kubernetes sends SIGTERM
        │
        ▼
   Stop accepting new requests
        │
        ▼
   Finish in-flight requests (timeout: 30s)
        │
        ▼
   Close DB connections
        │
        ▼
   Process exits cleanly
        │
        ▼
   K8s kills container after grace period
```

---

## 15. CI/CD pipeline

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   git push origin feature/x                              │
   │              │                                           │
   │              ▼                                           │
   │   ┌──────────────────────────────────────────┐           │
   │   │ GitHub Actions / GitLab CI               │           │
   │   │                                          │           │
   │   │  Parallel jobs:                          │           │
   │   │  ┌──────────────┐ ┌──────────────────┐  │           │
   │   │  │ Lint         │ │ Tests            │  │           │
   │   │  │ - cargo fmt  │ │ - postgres in    │  │           │
   │   │  │ - clippy     │ │   container       │  │           │
   │   │  │ - audit      │ │ - cargo test     │  │           │
   │   │  └──────────────┘ └──────────────────┘  │           │
   │   │  ┌──────────────┐ ┌──────────────────┐  │           │
   │   │  │ Coverage     │ │ Doc tests        │  │           │
   │   │  │ - llvm-cov   │ │                  │  │           │
   │   │  └──────────────┘ └──────────────────┘  │           │
   │   └────────────────┬─────────────────────────┘           │
   │                    │                                     │
   │                    ▼ if PR merged to main                │
   │   ┌──────────────────────────────────────────┐           │
   │   │ Build & Push Docker image                │           │
   │   │ - docker buildx                          │           │
   │   │ - push to ghcr.io                        │           │
   │   └────────────────┬─────────────────────────┘           │
   │                    │                                     │
   │                    ▼                                     │
   │   ┌──────────────────────────────────────────┐           │
   │   │ Deploy to staging                        │           │
   │   │ - update K8s deployment                  │           │
   │   │ - smoke test                             │           │
   │   └────────────────┬─────────────────────────┘           │
   │                    │                                     │
   │                    ▼ manual approval                     │
   │   ┌──────────────────────────────────────────┐           │
   │   │ Deploy to production                     │           │
   │   │ - canary deployment (5% → 100%)          │           │
   │   │ - monitor metrics                        │           │
   │   │ - auto-rollback on errors                │           │
   │   └──────────────────────────────────────────┘           │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
```

---

## 16. Performance considerations

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   COMMON BOTTLENECKS:                                    │
   │                                                          │
   │   ┌──────────────────┬───────────────┬──────────────┐    │
   │   │ Symptom          │ Cause         │ Fix          │    │
   │   ├──────────────────┼───────────────┼──────────────┤    │
   │   │ High P99 latency │ DB slow query │ EXPLAIN +    │    │
   │   │                  │               │ index        │    │
   │   │                  │               │              │    │
   │   │ Memory growing   │ No cache TTL  │ moka with    │    │
   │   │                  │ + Vec grow    │ TTL + bounds │    │
   │   │                  │               │              │    │
   │   │ CPU high         │ Serialize     │ Cache JSON,  │    │
   │   │                  │ on each call  │ Bytes        │    │
   │   │                  │               │              │    │
   │   │ Conn errors      │ Pool exhausted│ Tune size,   │    │
   │   │                  │               │ shorter txs  │    │
   │   │                  │               │              │    │
   │   │ Cascading        │ No circuit    │ tower-load-  │    │
   │   │ timeouts         │ breaker       │ shed         │    │
   │   └──────────────────┴───────────────┴──────────────┘    │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Caching strategy:
   ─────────────────
   
   ┌─────────────────────────────────────────────┐
   │ Request → check Cache                       │
   │           │                                 │
   │      ┌────┴────┐                            │
   │     HIT       MISS                          │
   │      │         │                            │
   │      ▼         ▼                            │
   │   return    Query DB                        │
   │   cached         │                          │
   │              Insert cache                   │
   │                  │                          │
   │                  ▼                          │
   │              return                         │
   │                                             │
   │ Cache: moka in-memory + TTL                 │
   │   - 60s TTL                                 │
   │   - 10k entries max                         │
   │   - LRU eviction                            │
   └─────────────────────────────────────────────┘
   
   
   Connection pool sizing:
   ───────────────────────
   
   pool_size = (target_throughput / 1000ms) × avg_query_time_ms
   
   Example:
     target = 100 req/s
     avg query = 50ms
     pool = 100 × 0.050 = 5 connections (minimum)
     + buffer for spikes = 10-20 connections
   
   Too small → queue
   Too large → DB overload
```

---

## 17. Production checklist

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │   ☑ TLS termination (Caddy / nginx / rustls in-app)      │
   │   ☑ Tracing → log aggregator (Loki / Elasticsearch)      │
   │   ☑ Metrics → Prometheus scrape                          │
   │   ☑ Health checks /health, /ready                        │
   │   ☑ Graceful shutdown (SIGTERM handling)                 │
   │   ☑ Resource limits set (CPU, memory)                    │
   │   ☑ Config from env (no secrets in code)                 │
   │   ☑ Database migrations auto-run on deploy               │
   │   ☑ Connection pool sized appropriately                  │
   │   ☑ Cache layer (Redis or in-memory)                     │
   │   ☑ Rate limiting (per-IP or per-user)                   │
   │   ☑ Request timeout (per-request)                        │
   │   ☑ Body size limit                                      │
   │   ☑ CORS configured                                      │
   │   ☑ Error responses don't leak internals                 │
   │   ☑ Sensitive fields redacted in logs                    │
   │   ☑ Backup strategy for DB                               │
   │   ☑ Disaster recovery plan                               │
   │   ☑ Monitoring + alerting set up                         │
   │   ☑ Load tested for expected traffic                     │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
```

---

## 18. Applied chapters mapping

```
   ┌──────────────────────────────────────────────────────────┐
   │                                                          │
   │  CHAPTER             →  AXUM PROJECT USAGE              │
   │  ────────────────────────────────────────                │
   │                                                          │
   │  1.  memory-model     → AppState Arc layout              │
   │  2.  ownership        → handler signatures, moves        │
   │  3.  trait            → IntoResponse, Service,           │
   │                         FromRequestParts                 │
   │  4.  generic          → State<T>, Path<T>, Query<T>      │
   │  5.  closure          → middleware closures              │
   │  6.  async            → all handlers, sqlx, tokio        │
   │  7.  error-handling   → AppError + IntoResponse          │
   │  8.  macros           → query_as!, instrument, derive    │
   │  9.  smart-pointers   → Arc<AppState>, PgPool            │
   │  10. lifetime         → owned API, Arc avoids lifetime   │
   │  11. performance      → criterion, profile, caching      │
   │  12. observability    → tracing + Prometheus + OTel      │
   │  13. iterator         → result processing                │
   │  14. unsafe-rust      → typically NONE (libraries use)   │
   │  15. testing          → unit + integration + e2e         │
   │  16. embedded-rust    → not applicable (server context)  │
   │                                                          │
   └──────────────────────────────────────────────────────────┘
   
   
   Every chapter EXCEPT embedded applies to web service.
   This is the synthesis of all 15 prior chapters in production.
```

---

## 19. Mind map cuối

```
                          AXUM PROJECT
                                │
        ┌────────────┬──────────┼──────────┬─────────────┐
        ▼            ▼          ▼          ▼             ▼
   FRAMEWORK     STRUCTURE   FEATURES    INFRA        PRODUCTION
        │            │          │          │             │
   axum         workspace   Routing    sqlx         Docker
   tower        layers      Extractors Database     K8s
   hyper        api/domain  Middleware Migrations   CI/CD
   tokio        /db         Auth/JWT   Connection   Graceful
                            Validation  pool         shutdown
                            Errors                  TLS
                                                    Resource
                                                    limits
   
   
                ┌──────────────────────────────────────┐
                │  CORE INSIGHTS cho SENIOR            │
                │  ───────────────────────────         │
                │                                      │
                │  1. Workspace: api / domain / db     │
                │                                      │
                │  2. AppState clonable (Arc inside)   │
                │                                      │
                │  3. AppError + IntoResponse pattern  │
                │                                      │
                │  4. Don't leak internals to clients  │
                │                                      │
                │  5. Validate in extractor — DRY      │
                │                                      │
                │  6. sqlx compile-time SQL safety     │
                │                                      │
                │  7. Tracing span per request         │
                │                                      │
                │  8. 4 Golden Signals exposed         │
                │                                      │
                │  9. Test pyramid (unit > int > e2e)  │
                │                                      │
                │  10. Graceful shutdown SIGTERM       │
                │                                      │
                │  11. Config: env + file + validate   │
                │                                      │
                │  12. Production checklist passed     │
                └──────────────────────────────────────┘
```

---

## 20. Bộ tài liệu Rust giờ có 17 chủ đề

```
   ┌──────────────────────────────────────────────────────────┐
   │             RUST FOUNDATIONS LIBRARY                     │
   │  ──────────────────────────────────────────────────────  │
   │                                                          │
   │   1. memory-model            — Bộ nhớ                    │
   │   2. ownership-borrowing     — Sở hữu cơ bản            │
   │   3. trait                   — Polymorphism             │
   │   4. generic                 — Parametric polymorphism  │
   │   5. closure                 — Function as value        │
   │   6. async                   — Concurrency              │
   │   7. error-handling          — Error handling           │
   │   8. macros                  — Macros                   │
   │   9. smart-pointers          — Smart pointers            │
   │  10. lifetime                — Lifetime deep dive       │
   │  11. performance             — Profile & optimize       │
   │  12. observability           — Logs/Traces/Metrics      │
   │  13. iterator                — Iterator + Stream + Rayon │
   │  14. unsafe-rust             — Unsafe + FFI + Atomic    │
   │  15. testing                 — Testing patterns         │
   │  16. embedded-rust           — Embedded + RTIC + Embassy │
   │  17. axum-project            — Realistic web project    │
   │      axum-project-visual     ← VỪA HOÀN THÀNH           │
   │                                                          │
   │  ──────────────────────────────────────────────────────  │
   │                                                          │
   │   🦀 Bộ kỹ năng Rust production-ready FULL STACK        │
   │   Từ embedded MCU đến web server đến database service    │
   └──────────────────────────────────────────────────────────┘
```

---

## Chủ đề tiếp theo gợi ý

Còn 1 topic ứng dụng cuối:

- **Database deep dive** — sqlx vs sea-orm vs diesel, transaction patterns, migration strategies, connection pool tuning, query optimization, advanced patterns

Báo nếu muốn đào sâu! 🦀⚡
